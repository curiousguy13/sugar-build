#!/usr/bin/env python3.3

import os
import shutil

import common

common.run("clean")

shutil.rmtree(common.logs_dir, ignore_errors=True)

os.unlink(os.path.join(common.cache_dir, "common.cpython-33.pyc"))
